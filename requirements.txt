--extra-index-url https://download.pytorch.org/whl/cpu

# ── Web Framework ─────────────────────────────────────────────────────────────
flask==3.0.0
werkzeug>=3.0.0,<4.0.0       # Imported directly in app.py; Flask 3.x requires Werkzeug 3.x
gunicorn==21.2.0

# ── Chess Logic ───────────────────────────────────────────────────────────────
chess==1.10.0

# ── AI / ML ───────────────────────────────────────────────────────────────────
torch>=2.2.0,<3.0.0          # CNN inference; >=2.2 required for torch.softmax double() stability
numpy>=1.26.0,<2.0.0         # Pin below 2.0: numpy 2.x has breaking C-API changes affecting torch/pandas
scikit-learn>=1.3.0,<2.0.0   # Random Forest for Scout feature

# ── Data ──────────────────────────────────────────────────────────────────────
pandas==2.1.4                 # Pin to 2.x; pandas 3.x has breaking API changes (e.g. DataFrame.groupby)
duckdb>=0.10.0,<2.0.0        # Puzzle database; 0.10+ required for ILIKE support
joblib==1.3.2                 # Model serialization (.pkl files)

# ── Hugging Face ──────────────────────────────────────────────────────────────
huggingface_hub>=0.20.0,<2.0.0  # Dataset sync in analytics.py (upload_file, hf_hub_download)

# ── Utilities ─────────────────────────────────────────────────────────────────
requests==2.31.0              # Lichess & Chess.com API calls in RF_model/predict.py
python-dotenv==1.0.0          # Local .env loading